v1.0
- Make all matching fuzzy (except for opt-in)

v1.1
- Alias support
- Automatic `git status` sanity check to better detect repo changes (and needs to push/etc)
- Don't hardcode "password" as the only field to use the PasswordEditor

FUTURE
- Single file import/export format to enable backups
- Consider change to symmetric file format encryption: either scrypt or GPG symmetric to make setup easier
- Make all CLI verbs default to prompt interactively for the key to edit to reduce information in your shel history
- Improve git remote integration to better handle multi-system use
